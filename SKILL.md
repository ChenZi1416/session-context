---
name: session-context
description: 会话上下文存取（WPS 云文档后端）。当用户要求"保存上下文"、"保存会话"、"总结会话"、"save context"、"恢复上下文"、"恢复会话"、"restore context"、提到特征码（如 CTX-YYYYMMDD-XXXX）、"列出上下文"、"查看历史会话"时触发本技能。数据持久化到 WPS 云文档，支持跨设备恢复。
---

# 会话上下文存取

在会话结束时保存当前对话的上下文摘要到 WPS 云文档，并在新会话中通过"特征码"快速恢复，无需重述整个对话历史。数据存储在云端，支持跨设备访问。

## 存储位置

- **云端**：WPS 云文档根目录下 `会话上下文/` 文件夹，每份快照为一个 `CTX-YYYYMMDD-XXXX.json` 文件
- **本地缓存**：`~/.lingxi-context/`，仅用于上传/下载时的临时文件，运行结束后自动清理

## 脚本路径

核心脚本：`<skill_scripts>/context_manager.py`

`<skill_scripts>` 指技能目录下 `scripts/` 的实际路径。脚本内部自动加载 kdocs 模块，无需额外配置。

## 工作模式

### 保存上下文

当用户要求保存当前会话上下文时：

1. **回顾当前会话**，提炼以下信息：
   - 会话主题和目标
   - 讨论的关键内容和关键决策
   - 已完成的任务和产出的文件（含路径）
   - 未完成的待办事项
   - 用户表达的偏好、纠正或特殊要求
   - 下一步计划

2. **按以下结构组织摘要**（Markdown 格式）：

   ```markdown
   ## 会话主题
   [一句话描述本次会话的核心主题]

   ## 关键决策
   - [决策1]
   - [决策2]

   ## 已完成
   - [任务1] -> 产出: [文件路径]
   - [任务2]

   ## 待办事项
   - [待办1]
   - [待办2]

   ## 用户偏好/纠正
   - [偏好或纠正记录]

   ## 重要上下文
   [恢复会话所需的任何额外背景信息]
   ```

3. **将摘要写入临时 JSON 文件**（用 `file` 工具），格式如下：

   ```json
   {
     "title": "简短标题",
     "tags": ["标签1", "标签2"],
     "summary": "上面的 Markdown 摘要全文"
   }
   ```

4. **执行保存脚本**：

   ```bash
   python <skill_scripts>/context_manager.py save <json_file_path>
   ```

5. **告知用户特征码**：脚本输出形如 `CTX-20260808-A3F2` 的特征码及云文档链接。告知用户特征码，并提示在新会话中发送该特征码即可恢复上下文。

### 恢复上下文

当用户提供特征码（如 `CTX-20260808-A3F2`）要求恢复上下文时：

1. **执行恢复脚本**：

   ```bash
   python <skill_scripts>/context_manager.py restore <feature_code>
   ```

2. **解析输出**：脚本从云端下载快照并返回 JSON，包含 `title`、`created_at`、`tags`、`summary` 等字段。

3. **将摘要内容作为当前会话的上下文**，向用户简要确认已恢复的内容：
   - 会话主题是什么
   - 之前做到哪了
   - 有哪些待办

4. **继续协作**：基于恢复的上下文继续工作。

### 列出所有上下文

当用户要求查看历史会话列表时：

```bash
python <skill_scripts>/context_manager.py list
```

脚本列出云端所有快照的特征码、云文档链接和创建时间。向用户以表格形式展示。

### 删除上下文

kdocs API 不支持直接删除云文档文件。当用户要求删除某个上下文快照时：

1. 先执行 `python <skill_scripts>/context_manager.py delete <feature_code>` 获取云端文件信息
2. 告知用户该快照的云文档链接，请用户在 WPS 云文档中手动删除

## 特征码格式

`CTX-YYYYMMDD-XXXX`

- `YYYYMMDD`：保存日期
- `XXXX`：4 位随机十六进制

示例：`CTX-20260808-A3F2`

## 注意事项

- 快照摘要的质量直接决定恢复效果，应确保包含恢复会话所需的关键信息
- 如果会话较长，摘要应聚焦于"恢复后继续工作需要知道什么"，而非逐条复述对话
- 产出的文件路径必须写完整路径，便于新会话中直接引用
- 数据存储在用户 WPS 云文档中，可跨设备访问；如更换设备，在新设备登录同一 WPS 账号即可
